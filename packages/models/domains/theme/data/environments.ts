import { EEnvironment } from '../enums/environment';

export const ENVIRONMENTS: Array<EEnvironment> = [
  EEnvironment.BLUE,
  EEnvironment.PINK,
  EEnvironment.RED,
  EEnvironment.GREEN,
  EEnvironment.YELLOW,
] as const;
