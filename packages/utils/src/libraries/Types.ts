export {
  toArray,
  toFinite,
  toInteger,
  toLength,
  toNumber,
  toPlainObject,
  toSafeInteger,
  toString,
} from 'lodash-es';

export const toBoolean = (value: any): boolean => Boolean(value);
